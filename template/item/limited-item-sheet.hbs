<form class="main-form main-form--limited {{cssClass}}">
    <div class="portrait">
        <img src="{{item.img}}" alt="{{item.name}} portrait">
        <button data-action="show-image"><i class="fas fa-image"></i></button>
    </div>
    <h2>{{item.name}}</h2>
    <div class="editor-wrapper">
        {{editor enrichedPublicNotes}}
    </div>
</form>